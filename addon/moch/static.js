/**
 * Static stream injections (e.g. "open in browser" links, special sources).
 * Returns an array of Stremio stream objects.
 */
export function toStaticStream(id, config) {
  // Currently no static streams; extend here as needed.
  return [];
}
